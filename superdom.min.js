var dom=(function nodeSelector(){let DOM=sel=>DOM[sel];let derivated=(selector,orig)=>new Proxy(orig,{get:(orig,name)=>{return DOM[selector(name)]},set:(orig,name,value)=>{DOM[selector(name)]=value;return true},deleteProperty:(orig,name)=>{delete DOM[selector(name)];return true}});let getter=(orig,key)=>{if(key in orig)return orig[key];if(key in orig.api.selectors)return derivated(orig.api.selectors[key],orig);return orig.api.array(orig.api.selectors(key))};let setter=(orig,key,value)=>{let cb=DOM.api.fn(value,true);DOM[key].each(node=>node.parentNode.replaceChild(cb(node)[0],node))};let deletter=(base,key)=>{DOM[key].forEach(n=>n.remove());return true};DOM.api={nodes:{}};DOM=new Proxy(DOM,{get:getter,set:setter,deleteProperty:deletter});return DOM})(dom);dom=(function Attributes(DOM){DOM.api.fn=(value,parse=false)=>{let cb=node=>parse ? DOM(value):value;if(value instanceof Function)cb=value;return cb};DOM.api.array=nodes=>{let getter=(orig,key)=>{if(key in orig){return orig[key]}let proxify=(()=>{let nodeCb=dom.api.nodes[key];if(nodeCb){if(nodeCb.get)nodeCb=nodeCb.get;return nodes.map((nodes,i,all)=>nodeCb(nodes,i,all))}return nodes.map(node=>node.getAttribute(key)|| '')})();proxify._={ref:nodes,attr:key};return DOM.api.values(proxify)};let setter=(orig,key,value)=>{let cb=DOM.api.fn(value);let nodeCb=dom.api.nodes[key];if(nodeCb){if(nodeCb.set)nodeCb=nodeCb.set;orig.map((node,i,all)=>nodeCb(cb,node,i,all));return true}if(value instanceof Function){cb=(node,i,orig)=>value(node.getAttribute(key)|| '',i,orig)}else{cb=node=>value}orig.forEach((node,i,orig)=>node.setAttribute(key,cb(node,i,orig)|| ''))};let deletter=(orig,key)=>{let cb=el=>el.removeAttribute(key);if(dom.api.nodes[key] && dom.api.nodes[key].del){cb=dom.api.nodes[key].del}orig.forEach(cb);return true};return new Proxy(nodes,{get:getter,set:setter,deleteProperty:deletter})};return DOM})(dom);dom=(function Values(DOM){var specialAttrs={_flat:lists=>[...new Set([].concat.apply([],lists))],_text:lists=>[...new Set([].concat.apply([],lists))].join(' ')};DOM.api.values=attributes=>{let getter=(orig,key)=>{if(key in orig || typeof orig[key] !=='undefined'){return orig[key]}let nodes=orig._.ref;let cb=DOM.api.attribute[orig._.attr];if(cb && cb.get){cb=cb.get;orig.map((attr,i,all)=>cb(attr,key,nodes[i],i,all))}if(key in specialAttrs){return specialAttrs[key](orig)}return specialAttrs._flat(orig).includes(key)};let setter=(orig,key,value)=>{let nodes=orig._.ref;let attrCb=dom.api.attribute[orig._.attr];if(attrCb){if(attrCb.set)attrCb=attrCb.set;orig.map((attr,i,all)=>attrCb(DOM.api.fn(value),key,nodes[i],i,all))}return true};let deletter=(orig,key)=>{let nodes=orig._.ref;let attrCb=dom.api.attribute[orig._.attr].del;if(attrCb){orig.map((attr,i,all)=>attrCb(key,nodes[i],i,all))}return true};return new Proxy(attributes,{get:getter,set:setter,deleteProperty:deletter})};return DOM})(dom);dom.api.selectors=(name='')=>/^\s*\</.test(name)? dom.api.generate(name):[...document.querySelectorAll(name)];dom.api.selectors.id=name=>`#${name}`;dom.api.selectors.class=name=>`.${name}`;dom.api.selectors.attr=name=>`[${name}]`;dom.api.generate=html=>{var type=/^\s*<t(h|r|d)/.test(html)? 'table':'div';var cont=document.createElement(type);cont.innerHTML=html.replace(/^\s*/,'').replace(/\s*$/,'');return [...cont.childNodes]};dom.api.nodes=nodes=>nodes;dom.api.nodes.each=(cb,node,i,all)=>cb(node,i,all);dom.api.nodes.html={get:node=>node.innerHTML,set:(cb,node,i,all)=>node.innerHTML=cb(node.innerHTML,i,all)|| '',del:node=>node.innerHTML=''};dom.api.nodes.class={get:node=>Array.from(node.classList),set:(cb,node,i,all)=>{let val=cb(Array.from(node.classList),i,all);val=typeof val==='string' ? dom.api.args(val):val;val.forEach(one=>node.classList.add(one))}};dom.api.args=val=>val.split(/[\s,]+/);dom.api.attribute={};dom.api.attribute.class={set:(cb,key,node,i,all)=>{let val=cb(node.classList.contains(key),i,all);node.classList[val ? 'add':'remove'](key)},del:(key,node)=>{node.classList.remove(key)}};dom.api.alias={};dom.api.alias.text='textContent';dom.api.nodes.text=(cb,node,i,all)=>node.textContent=cb(node.textContent,i,all)|| '';dom.api.replace=(nodes,value)=>{};if(typeof module !=='undefined'){module.exports=dom}